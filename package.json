{
  "name": "multi-regexp2",
  "version": "1.0.3",
  "main": "dist/multiRegExp2.js",
  "devDependencies": {
    "babel-cli": "^6.26.0",
    "babel-preset-es2015": "^6.22.0"
  },
  "scripts": {
    "build": "babel src --out-dir dist --source-maps --presets es2015 --watch",
    "test": "node dist/multiRegExp2-test.js",
    "best": "npm run build && npm run test"
  }
}
